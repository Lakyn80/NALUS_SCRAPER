# Copy to /opt/nalus-scraper/.env on WEDOS (server-only; never commit secrets).
# Used by docker-compose.court-staging.yml via ${VAR} substitution.
#
# Pin an explicit GHCR tag — do not rely on :latest for historical backfill jobs.
IMAGE_TAG=court-ns-backfill-d87026f
GHCR_OWNER=lakyn80
COURT_STAGING_HOST_DIR=/opt/nalus-data/court_staging
